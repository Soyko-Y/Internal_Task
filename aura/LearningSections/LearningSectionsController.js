({
	handleInitCmp: function(cmp, event, helper) {
		helper.doInit(cmp);
	},

	handleGetLearningSections: function(cmp, event, helper) {
		helper.getLearningSections(cmp);
	},

	handleGetDocument: function(cmp, event, helper) {
		helper.getDocument(cmp);
	}
})